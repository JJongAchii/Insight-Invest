# Insight-Invest API — qdata 소비자 재구조 (2026-07) 이후의 의존성.
# DB·ETL 스택(sqlalchemy/psycopg2/pyiceberg/boto3/yfinance 등) 전부 제거됨.

fastapi>=0.111
uvicorn>=0.30            # 로컬 실행용
mangum>=0.17             # Lambda 어댑터
pandas>=2.1
numpy>=1.26
polars>=1.0              # backtest/strategy 피벗
scipy>=1.11              # optimization (mean-variance, risk parity)
pyarrow>=15.0
s3fs>=2024.6             # APP_DATA·QDATA_LAKE s3:// 읽기/쓰기
boto3>=1.34              # Research Radar projection Lambda S3 client
httpx>=0.27              # news (Google News RSS)
anthropic>=0.40          # news briefing 큐레이션 (EC2 배치 — scripts/build_news.py)
pywebpush>=2.0           # iOS Home Screen PWA 포함 표준 Web Push

# qdata(데이터 계약)는 여기 넣지 않는다 — private repo라 토큰이 필요해서
# Dockerfile이 GH_TOKEN build arg로 별도 설치. 로컬 개발은:
#   pip install -e "../quant-data[s3]"
