# ADR-0034: ambiguous numeric claims stay held in the reading display

2026-09-15. Run 34917805437 exercised the corrected automatic evidence-to-writer
path for all three frozen originals. All source receipts recovered; Robeco's
rejected method is null and its actual credit comparison reaches why_read.

Two generated fields nevertheless failed human comparison despite model
`supported`: CFM finding repeats unnamed 0.38/0.18; D.E.Shaw why_read broadens a
simplified example's 3.2x into an apparent practical bound. These exact outputs
are preserved, not rerun until a favorable draw and not silently rewritten.

The existing field-hold projection gets version `reading-display-v2-numeric-scope`.
Decimal statements without a named metric present in both the point and its
attached evidence, or explicit units on the number in both, are held. Numeric
examples must retain explicit example scope in Korean. This conservative check
does not establish numerical truth, infer missing units, or supersede semantic
review. Typed correlation/Sharpe/p-values, explicit percentages/durations, and
qualitative statements have positive controls. No publisher/title/known number
blacklist is used.

Only those fields are hidden, with the existing partial/needs-check label. Other
supported points and original links remain available. The underlying draft,
review verdict, cost, source receipts, and cache identity are unchanged. The
projection replay must record the original report digest and zero LLM calls;
publication seeds original immutable receipts, never the replay as a new API run.

This is limited V1 reading acceptance, not a claim of complete summaries or a
perfect reviewer. No additional model calls or budget increase is needed for this
deterministic guard. Unknown metrics may require omission or later review.
