# ADR 0031 — Physically isolate evidence review input

Date: 2026-09-14. Status: implementation / source qualification.

## Observed failure, not a successful release

V4 / selector v7 classified all 15 fixed originals as expected (8 core, 5 context,
2 held), including the old ETF and SaaS failures. This is NOT semantic acceptance:
the good Robeco systematic-credit original was admitted on the unsupported premise
that 'ranks issuers and bonds according to their attractiveness' discloses a method.
The text actually teaches a spread-versus-issuer-risk comparison elsewhere. Correct
document labels for the wrong evidence do not establish a reliable boundary.

V4 spent $0.06619950 on 16 new calls; qualification ledger is $0.79056720. All old
receipts, frozen labels and failures remain. No V4 production release or cache seed.

## Two repairs, separately observable

1. Boundary v5 receives ONLY the proposed literal insight/method passages and the
   first reader's literal purpose passage. No title, publisher or surrounding body
   is sent. The first reader still sees the bounded full body and owns primary-topic
   selection. The checker cannot use unseen context to repair an insufficient quote.
   Its object citation must be an actually visible ID, not merely a valid ID elsewhere
   in the original. Receipts distinguish source input length from model-read length.
2. The selector must choose the disclosed analytic relation rather than a generic
   ranking-engine description. A generic universe/entity or undefined attractiveness
   score is not a measured input. A qualitative credit-risk comparison, sensitivity
   exercise or methodological pitfall is enough; full reproducibility is not required.

This does not add a paid stage, replace the model, change source admission scope or
claim deterministic semantic truth. It removes a concrete evidence-context leak.
The budget reservation is calculated on the actual reduced payload.

## Frozen pre-call contrast

First run boundary v5 against the preserved v7 positive sample: D.E.Shaw, CFM,
Robeco systematic credit. Do not regenerate the first selection. The Robeco weak
quote should not be accepted as operational detail; a hold will be a real original
selection false negative, not a reason to change its frozen positive label.

Then improve source evidence selection and rerun the same 15-original panel without
changing the original expectations. Add separately frozen new-to-panel examples
before their calls. Use the same prompt versions for all documents in a panel; do
not combine favorable decisions from old and new models/prompts. Inspect accepted
quotes/roles, not only route matches. A good original requires a good accepted
premise before release. No article is deleted because a proposal is insufficient.

Remaining qualification cap before this contrast: $0.20943280 of the existing $1,
aggregate monthly production+qualification still $2. No reset or automatic increase.
